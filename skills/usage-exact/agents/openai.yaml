interface:
  display_name: "Usage Exact"
  short_description: "Show Codex quota resets to the second"
  default_prompt: "Use $usage-exact to report my current Codex quota resets in Beijing time."

policy:
  allow_implicit_invocation: true
